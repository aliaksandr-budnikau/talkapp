subprojects {
    apply plugin: 'java'

    version '0.1'

    apply plugin: 'idea'
    apply plugin: 'eclipse'

    repositories {
        jcenter()
    }

    dependencies {
        testCompile 'junit:junit:4.12'
        compile 'org.slf4j:slf4j-api:1.7.25'
    }
}